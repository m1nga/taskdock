schema_version: "1.1"
name: organize-with-undo
description: Reorganize a task folder around the approved result, keep history, repair links, keep an undo path. 30 turns for both arms; a reversible reorganization plus a report needs the room.
tags: [organize, core]
execution:
  max_turns: 30
  allowed_tools: [Read, Glob, Grep, Skill, Bash, Write, Edit]
context:
  scaffold_script: fixture.sh
