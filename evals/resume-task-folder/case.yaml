schema_version: "1.1"
name: resume-task-folder
description: Resume an existing task folder where the newest draft is not the approved version.
tags: [resume, core]
execution:
  max_turns: 14
  allowed_tools: [Read, Glob, Grep, Skill, Bash]
context:
  scaffold_script: fixture.sh
